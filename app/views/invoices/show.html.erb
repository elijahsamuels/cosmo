show the individual invoice for a job based on the current_user
include job details
<br>
<br>
<%# byebug %>
<br>Invoice Amount: <%= number_to_currency(@invoice.invoice_amount)%>
<br>Date Invoice will be sent in: <%= @invoice.date_invoice_sent.strftime("%a, %b %d, %Y at %l:%M%P") %>
<br>User ID: <%= @invoice.user_id %>
<br>Job ID: <%= @invoice.job_id %>


	<%#= form.hidden_field :job_id, :value => @job.id %>
	<%#= form.hidden_field :user_id, :value => current_user.id %>

	<br><%#= form.label :invoice_amount, "Invoice Amount" %>
	<br><%#= form.number_field :invoice_amount %>

	<%#= form.submit "Submit Payment" %>

